--[[
	Put the name of the SynArray folder into `requireString` and in addRequirePath add where to search for `requireString` which will be added to package.path when requiring SynArray.

	See the installation section of README.md if you still don't know what to do.
]]--

return {
	requireString = "synarray",
	addRequirePath = "/home/xxoa/Code/SynArray/version/stable/synarray-v1.0.00/?/init.lua"
}
